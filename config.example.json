{
  "model": "zhipuai-coding-plan/glm-4.6v",
  "prompt": "Describe this image in detail, including: text content, UI layout structure, interface elements, color scheme. If there are code or technical details, list them thoroughly.",
  "timeout": 60000,
  "limit": 5,
  "max_limit": 20,

  "_comment_plugin": "For opencode.json plugin config, use the section below instead of the above MCP fields",
  "_plugin_example": [
    "@showlotus/opencode-image-vision",
    {
      "model": "zhipuai-coding-plan/glm-4.6v",
      "unsupportedModels": [],
      "timeout": 30000
    }
  ]
}
